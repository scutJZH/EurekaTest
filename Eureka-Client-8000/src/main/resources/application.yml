server:
  port: 8000


eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://127.0.0.1:7000/eureka,http://127.0.0.1:7001/eureka
    healthcheck:
      enabled: true
spring:
  application:
    name: client-8000